version: "3.9"
services:
  circuits:
    build: .
    image: circuits-app
    ports:
      - "8000:8000"
    volumes:
      - ./circuits.db:/app/circuits.db
    environment:
      - UVICORN_WORKERS=1
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
